<!-- TODO: Fix entire index view -->
<% provide(:title, 'All users') %>
<h1>All users</h1>

<!-- TODO: Format pagination -->
<%= will_paginate %>
<ul class="users">
  <% @users.each do |user| %>
      <%= render user %>
  <% end %>
</ul>
<%= will_paginate %>